namespace oopseg
{
    interface Icalc  //by default internal i.e. available in the namespace
    {
        int add(int x,int y);
        int sub(int x,int y);
        int mul(int x,int y);
        int div(int x,int y);
    }
}